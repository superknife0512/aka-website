<%- include('./includes/html-head.ejs') %>
    <%- include('./includes/client-header.ejs') %>
        <div class="homepage-popup " v-if="isActive">
            <div class="homepage-popup__body flapInAnim">
                    <div class="homepage-popup__hot">HOT!! Don't miss</div>

                <h2 class="heading heading--5 text-center">
                    Đừng bỏ quên các khuyến mãi!!!
                </h2>
                <p>Ak thường xuyên đưa ra các tin tức nóng hổi về các khóa học được khuyến mãi 
                    cùng với đó là các sự kiện hỗ trợ cộng đồng,... các bạn không nên bỏ qua.
                </p>
                <div class="d-flex justify-content-center mt-4 homepage-popup__btn-group">
                    <button class="corner-btn mr-4" 
                            @click="isActive = false">Để sau</button>
                    <a href="/event-page"
                        target="blank" 
                        class="link-btn">Xem ngay!!!</a>
                </div>
            </div>

        </div>

        <div class="grid">
            <div class="grid__container ">
                <div class="course-page__heading">
                    <h2 class="heading heading--1">
                        Các khóa học của chúng tôi
                    </h2>
                    <p class="text-center">
                        Một chút thông tin về các khóa học của AK. Lorem ipsum dolor, sit amet consectetur adipisicing elit. Atque, hic magnam dolorem facere soluta quo! Iure, nam! Officia, veniam. Amet laboriosam quaerat vitae sed tenetur voluptas ab saepe cupiditate eligendi! Obcaecati ipsam eum fugiat ex ducimus quibusdam, laborum fugit quisquam tempore optio delectus sint consequuntur repellat ullam doloremque voluptatem reprehenderit ipsa dolorem.
                    </p>
                </div>
                <div class="course-page__body">
                    <h2 class="heading heading--5">
                        Các khóa học tiếng trung
                    </h2>
                    <% if(courseChi[0]) { %>
                            <div class="course-page__display">
                            <% courseChi.forEach((course,i)=>{ %>
                                <%- include('./includes/course-card.ejs', {course, i, saleOff: discountChi, titleSEOs: titleSEOChi}) %>
                            <% }) %>
                        </div>
                    <% } else { %>
                        <h3>Hiện chúng tôi chưa có khóa học vào về tiếng Trung!</h3>
                    <% } %>
                    
                </div>
                <hr>
                <div class="course-page__body">
                    <h2 class="heading heading--5">
                        Các khóa học tiếng Anh
                    </h2>
                    <% if(courseEng[0]) { %>

                        <div class="course-page__display">
                            <% courseEng.forEach((course,i)=>{ %>
                                <%- include('./includes/course-card.ejs', {course, i, saleOff: discountEng, titleSEOs: titleSEOEng}) %>
                            <% }) %>
                        </div>

                    <% } else { %>
                        <h3>Hiện chúng tôi chưa có khóa học vào về tiếng Anh!</h3>
                    <% } %>
                </div>
                <hr>
                <div class="course-page__body">
                    <h2 class="heading heading--5">
                        Các khóa học tiếng Nhật
                    </h2>
                    <% if(courseJav[0]) { %>
                         <div class="course-page__display">
                            <% courseJav.forEach((course,i)=>{ %>
                                <%- include('./includes/course-card.ejs', {course, i, saleOff: discountJav, titleSEOs: titleSEOJav}) %>
                            <% }) %>
                        </div>
                    <% } else { %>
                        <h3>Hiện chúng tôi chưa có khóa học vào về tiếng Nhật!</h3>
                    <% } %>
                   
                </div>
                <hr>

                <div class="course-page__body">
                    <h2 class="heading heading--5">
                        Các khóa học tiếng Hàn
                    </h2>
                    <% if(courseKor[0]) { %>
                         <div class="course-page__display">
                            <% courseKor.forEach((course,i)=>{ %>
                                <%- include('./includes/course-card.ejs', {course, i, saleOff: discountKor, titleSEOs: titleSEOKor}) %>
                            <% }) %>
                        </div>
                    <% } else { %>
                        <h3>Hiện chúng tôi chưa có khóa học vào về tiếng Hàn!</h3>
                    <% } %>
                   
                </div>
        </div>

        <script>
            const app = new Vue({
                el: '.homepage-popup',
                data:{
                    isActive:false
                },
                created(){
                    setTimeout(()=>{
                        this.isActive = true;
                    }, 1000)
                }
            })
        </script>
    <%- include('./includes/client-footer.ejs') %>
<%- include('./includes/footer.ejs') %>